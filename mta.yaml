_schema-version: "3.2"
ID: appcatalog
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appcatalog-destination-content
  type: com.sap.application.content
  requires:
  - name: appcatalog-destination-service
    parameters:
      content-target: true
  - name: appcatalog-repo-host
    parameters:
      service-key:
        name: appcatalog-repo-host-key
  - name: appcatalog-uaa
    parameters:
      service-key:
        name: appcatalog-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appcatalog_html_repo_host
          ServiceInstanceName: appcatalog-html5-service
          ServiceKeyName: appcatalog-repo-host-key
          sap.cloud.service: appcatalog
        - Authentication: OAuth2UserTokenExchange
          Name: appcatalog_uaa
          ServiceInstanceName: appcatalog-xsuaa-service
          ServiceKeyName: appcatalog-uaa-key
          sap.cloud.service: appcatalog
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: appcatalog-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appcatalog-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appcatalog.zip
      name: appcatalog
      target-path: resources/
- name: appcatalog
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appcatalog-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appcatalog-destination-service
    service-plan: lite
- name: appcatalog-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: appcatalog-xsuaa-service
    service-plan: application
- name: appcatalog-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appcatalog-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
